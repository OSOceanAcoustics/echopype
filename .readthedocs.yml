version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
    nodejs: "22"

sphinx:
  configuration: docs/source/conf.py

formats: []

python:
  install:
    - method: uv
      command: sync
      extras:
        - plot
      groups:
        - docs
